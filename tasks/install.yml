# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Ensure Actual paths exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0750"
    owner: "{{ actual_uid }}"
    group: "{{ actual_gid }}"
  with_items:
    - "{{ actual_base_path }}"
    - "{{ actual_data_path }}"

- name: Ensure Actual support files installed
  ansible.builtin.template:
    src: "{{ role_path }}/templates/{{ item }}.j2"
    dest: "{{ actual_base_path }}/{{ item }}"
    mode: "0640"
    owner: "{{ actual_uid }}"
    group: "{{ actual_gid }}"
  with_items:
    - env
    - labels
  register: actual_support_files_result

- name: Ensure Actual container image is pulled via community.docker.docker_image
  when: devture_systemd_docker_base_container_image_pull_method == 'ansible-module'
  community.docker.docker_image:
    name: "{{ actual_container_image }}"
    source: "{{ 'pull' if ansible_version.major > 2 or ansible_version.minor > 7 else omit }}"
    force_source: "{{ actual_container_image_force_pull if ansible_version.major > 2 or ansible_version.minor >= 8 else omit }}"
    force: "{{ omit if ansible_version.major > 2 or ansible_version.minor >= 8 else actual_container_image_force_pull }}"
  register: actual_container_image_pull_result
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: actual_container_image_pull_result is not failed

- name: Ensure Actual container image is pulled via ansible.builtin.command
  when: devture_systemd_docker_base_container_image_pull_method == 'command'
  ansible.builtin.command:
    cmd: "{{ devture_systemd_docker_base_host_command_docker }} pull {{ actual_container_image }}"
  register: actual_container_image_pull_result
  retries: "{{ devture_playbook_help_container_retries_count }}"
  delay: "{{ devture_playbook_help_container_retries_delay }}"
  until: actual_container_image_pull_result is not failed
  changed_when: "'Downloaded newer image' in actual_container_image_pull_result.stdout"

- name: Ensure Actual container network is created via community.docker.docker_network
  when: devture_systemd_docker_base_container_network_creation_method == 'ansible-module'
  community.docker.docker_network:
    enable_ipv6: "{{ devture_systemd_docker_base_ipv6_enabled }}"
    name: "{{ actual_container_network }}"
    driver: bridge
    driver_options: "{{ devture_systemd_docker_base_container_networks_driver_options }}"
  register: actual_network_creation_result

- name: Ensure Actual container network is created via ansible.builtin.command
  when: devture_systemd_docker_base_container_network_creation_method == 'command'
  ansible.builtin.command:
    cmd: >-
      {{ devture_systemd_docker_base_host_command_docker }} network create
      {% if devture_systemd_docker_base_ipv6_enabled %}--ipv6{% endif %}
      {{ devture_systemd_docker_base_container_networks_driver_options_string }}
      {{ actual_container_network }}
  register: actual_network_creation_result
  changed_when: actual_network_creation_result.rc == 0
  failed_when: actual_network_creation_result.rc != 0 and 'already exists' not in actual_network_creation_result.stderr

- name: Ensure Actual systemd service is present
  ansible.builtin.template:
    src: "{{ role_path }}/templates/systemd/actual.service.j2"
    dest: "{{ devture_systemd_docker_base_systemd_path }}/{{ actual_identifier }}.service"
    mode: "0644"
  register: actual_systemd_service_result

- name: Determine whether Actual needs a restart
  ansible.builtin.set_fact:
    actual_restart_necessary: >-
      {{
        actual_support_files_result.changed | default(false)
        or actual_systemd_service_result.changed | default(false)
        or actual_container_image_pull_result.changed | default(false)
      }}
